# Final Outputs

A real finalized run writes deterministic outputs here:

```text
spritesheet.png
spritesheet.webp
validation.json
package-validation.json
```

Only `spritesheet.webp` is part of the runtime package. `spritesheet.png` and
validation files are local QA evidence unless deliberately promoted into docs.

