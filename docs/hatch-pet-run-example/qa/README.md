# QA Outputs

A real finalized run writes review artifacts here:

```text
contact-sheet.png
review.json
run-summary.json
videos/*.mp4
```

Inspect these before accepting a pet. Deterministic validation is necessary but
not sufficient; reject rows that change identity, silhouette, palette, markings,
prop placement, or rendering style.

